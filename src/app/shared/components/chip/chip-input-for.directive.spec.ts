import { ChipInputForDirective } from './chip-input-for.directive';

describe('ChipInputForDirective', () => {
  it('should create an instance', () => {
    const directive = new ChipInputForDirective();
    expect(directive).toBeTruthy();
  });
});
